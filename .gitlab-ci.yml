image: docker:latest

services:
  - docker:dind

stages:
  - package
  - build

maven-package:
  image: maven:3-jdk-8
  stage: package
  script:
    - mvn package
  artifacts:
    paths:
      - target/*.jar

docker-build:
  stage: build
  script:
    - docker build . -t store